version: '3.8'

services:
  expensepilot-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: expensepilot-backend
    ports:
      - "8080:8080"
    environment:
      # H2 Database (default for development)
      - SPRING_DATASOURCE_URL=jdbc:h2:mem:expensepilot
      - SPRING_DATASOURCE_DRIVER_CLASS_NAME=org.h2.Driver
      - SPRING_H2_CONSOLE_ENABLED=true
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
      - SPRING_JPA_SHOW_SQL=true
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped

  # Uncomment below for MySQL setup
  # mysql-db:
  #   image: mysql:8.0
  #   container_name: expensepilot-mysql
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=rootpassword
  #     - MYSQL_DATABASE=expensepilot
  #     - MYSQL_USER=expensepilot
  #     - MYSQL_PASSWORD=password123
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #   restart: unless-stopped

  # expensepilot-app-mysql:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: expensepilot-backend-mysql
  #   depends_on:
  #     - mysql-db
  #   ports:
  #     - "8081:8080"
  #   environment:
  #     # MySQL Database configuration
  #     - SPRING_DATASOURCE_URL=jdbc:mysql://mysql-db:3306/expensepilot?useSSL=false&serverTimezone=UTC
  #     - SPRING_DATASOURCE_USERNAME=expensepilot
  #     - SPRING_DATASOURCE_PASSWORD=password123
  #     - SPRING_DATASOURCE_DRIVER_CLASS_NAME=com.mysql.cj.jdbc.Driver
  #     - SPRING_JPA_HIBERNATE_DDL_AUTO=update
  #     - SPRING_JPA_SHOW_SQL=true
  #   restart: unless-stopped

# volumes:
#   mysql_data:
